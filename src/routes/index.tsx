/*
a pagina blog com os artigos não está disponicel para criar o link no menu
*/
import { createBrowserRouter } from "react-router-dom";

// This file seems to have been overwritten by a literal text request.
// I will restore a basic routing structure if it's missing, but I need to check App.tsx first to see how routes are handled.
export const router = createBrowserRouter([]);
